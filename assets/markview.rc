// Windows 资源脚本。由 build.rs 交给 rc.exe 编译，链接器再把结果写进 PE 文件。
//
// 资源 id 用 1：资源管理器取"应用图标"时，拿的是编号最小的那个图标组，
// 所以这里不要改成别的数字，也不要在它前面再塞一个 ICON。
//
// 文件名是相对**仓库根目录**的（rc.exe 的工作目录就是那里），不是相对本文件。
1 ICON "assets/AppIcon.ico"
